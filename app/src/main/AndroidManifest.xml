<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.home.androidarchitecturecomponentexercise"
    >

    <application
        android:allowBackup="true"
        android:icon="@drawable/mvvm"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mvvm"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidArchitectureComponentExercise"
        >
        <activity android:name=".AddEditNoteActivity"
            android:parentActivityName=".MainActivity"></activity>
        <activity android:name=".MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>